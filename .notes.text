# DIFFERENCE

• Blade Templating
  - Lama: inheritance
  - Baru: component



*NEW KNOWLEDGE

  # Controller
    - Closure function membuat scope baru, sehingga perlu keyword use untuk "membawa" variabel dari luar
    - Arrow function (fn) otomatis mewarisi scope dari function parent, jadi tidak perlu use


  # Route Model Binding
    - dengan extends Model pada Model maka secara otomatis Model yang kita buat terhubung dengan table yang ada di database
    - route binding langsung menggunakan nama model:nama_kolom

  # N + 1
    - ada 2 cara untuk menangani lazy loading : > pada setiap controller ditambahkan 1 1
                                                > mengguanakan eager loading default pada model  (lebih simple, karena hanya menambahkan 1 baris)




* terminal
  - php artisan make:model Category -mf                     ~> untuk membuat model sekaligus migration dan factorynya


  # untuk membersihkan cache
  - composer dumpautoload
  - php artisan optimize:clear


*sisanya ada di masing-masing file (model, controller dll)
