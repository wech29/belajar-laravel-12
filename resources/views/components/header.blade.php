@if ($title)
    <header class="relative bg-white shadow-sm">
        <div class="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8">
            <h3 class="text-3xl font-bold tracking-tight text-gray-900">{{ $title }}</h3>
        </div>
    </header>
@endif
